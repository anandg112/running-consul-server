### Running consul server in docker


#### Execute shell script

```./consul-server.sh consul-server consul-client```


#### Running on localhost

Accesible on:

```localhost:8500```

IP address of the consul server: 
```docker exec badger consul members```

